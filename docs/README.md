# 开发资料

- `verification/`：各功能的历史验证报告，仅反映报告记录的版本与测试结果。
- `ui-module-map.json`：UI 模块与事件文件清单。
- `../preview/`：游戏预览截图，包括从项目根目录迁入的截图。

JSON 报告和模块清单中的文件路径统一相对于项目根目录（`project.gdevelop` 所在目录），不是相对于当前文档目录。

自动化测试配置仍位于根目录 `tests.settings`，测试源码位于 `tests/`。
